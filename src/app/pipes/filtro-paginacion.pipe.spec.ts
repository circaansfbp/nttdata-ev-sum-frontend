import { FiltroPaginacionPipe } from './filtro-paginacion.pipe';

describe('FiltroPaginacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPaginacionPipe();
    expect(pipe).toBeTruthy();
  });
});
